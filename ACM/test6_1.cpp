#include <stdio.h>
#include <string.h>

int main() {
    int need[100];
    int last = 0;  // ????????????????
    
    for (int i = 0; i <= 1000; i++) {
        char h[5];  // 4¦Ë??+???????
        int sum = 0;
        
        // ???????¦Ë??
        sprintf(h, "%d", i);
        int now = strlen(h);
        
        // ????¦Ë???????????????
        int d = i;
        for (int b = 0; b < now; b++) {
            int digit = d % 10;  // ?????????¦Ë
            sum += digit * digit * digit;
            d /= 10;  // ???????¦Ë
        }
        
        // ?›¥????????????
        if (sum == i) {
            need[last] = i;
            last++;  // ??›¥??????
        }
    }
    
    // ??????
    for (int g = 0; g < last; g++) {
        printf("%d ", need[g]);
    }
    return 0;
}
